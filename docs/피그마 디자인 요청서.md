# SensePoint 피그마 디자인 요청서

> 기민이에게: 이 문서는 SensePoint 프로젝트의 현재 상태와 디자인 작업에 필요한 모든 정보를 담고 있어.
> 디자인 완료 후 Cursor AI로 코드에 바로 적용할 예정이라, 아래 가이드라인을 따라주면 적용이 훨씬 수월해!

---

## 1. 프로젝트 소개

**SensePoint** — 감각의 교차점을 탐험하며 인사이트를 발견하는 웹 플랫폼

- 다크 테마 기반 (검정 배경 + 골드 액센트)
- PC / 태블릿 / 모바일 반응형
- 현재 HTML/CSS/JS로 기능이 선행 구현되어 있고, 디자인을 입히는 단계

---

## 2. 디자인해야 할 페이지 목록

| # | 페이지 | 파일명 | 현재 상태 | 디자인 우선순위 |
|---|--------|--------|-----------|---------------|
| 1 | **랜딩 페이지** | index.html | 기능 구현 완료, 디자인 필요 | ★★★ |
| 2 | **Sense Makers** | sense-makers.html | 기능 구현 완료, 디자인 필요 | ★★★ |
| 3 | **Square** | square.html | 기능 구현 완료, 디자인 필요 | ★★☆ |
| 4 | **My Note** | my-note.html | 기획 미완, 디자인부터 제안 가능 | ★★☆ |
| 5 | **로그인** | login.html | 기능 구현 완료, 디자인 필요 | ★☆☆ |
| 6 | **회원가입** | signup.html | 기능 구현 완료, 디자인 필요 | ★☆☆ |

---

## 3. 반응형 브레이크포인트 (3가지 프레임 필요)

| 디바이스 | 피그마 프레임 너비 | 비고 |
|---------|-------------------|------|
| **PC** (디폴트) | **1440px** | 기본 레이아웃 |
| **태블릿** | **768px** | 중간 레이아웃 |
| **모바일** | **375px** | 최소 레이아웃 |

> 각 페이지마다 **PC / 태블릿 / 모바일** 3개의 프레임을 만들어줘.
> 여유가 없으면 **PC + 모바일** 2개만이라도 OK.

---

## 4. 현재 디자인 시스템 (참고용, 변경 가능)

디자인하면서 아래 값을 그대로 쓰거나, 더 나은 값으로 바꿔도 돼.
**바꿨다면 어떤 값을 바꿨는지 알려주면** Cursor로 바로 반영 가능.

### 4-1. 색상 팔레트

#### 배경
| 이름 | 현재 값 | 용도 |
|------|---------|------|
| Background | `#0a0a0a` | 메인 배경 |
| Background Secondary | `#111111` | 보조 배경, 메뉴 패널 |
| Surface | `#1a1a1a` | 카드/패널 배경 |
| Surface Hover | `#252525` | 호버 시 |

#### 텍스트
| 이름 | 현재 값 | 용도 |
|------|---------|------|
| Text Primary | `#f5f5f5` | 메인 텍스트 |
| Text Secondary | `#999999` | 보조 텍스트 |
| Text Muted | `#666666` | 비활성 텍스트 |
| White | `#ffffff` | 순백 |

#### 액센트
| 이름 | 현재 값 | 용도 |
|------|---------|------|
| Accent (Gold) | `#c8a96e` | 브랜드 포인트 컬러 |
| Accent Hover | `#d4b87a` | 호버 |

#### 기능 컬러
| 이름 | 현재 값 | 용도 |
|------|---------|------|
| Highlight Light | `rgba(255, 235, 153, 0.25)` | 메모 하이라이트 (옅은 노란색) |
| Highlight Active | `rgba(255, 210, 60, 0.5)` | 선택 중 하이라이트 (진한 노란색) |
| Border | `#2a2a2a` | 구분선 |
| Overlay | `rgba(0, 0, 0, 0.4)` | 메뉴 backdrop |

#### 태그 컬러 (5가지)
| 이름 | 값 |
|------|-----|
| Coral | `#e8857a` |
| Blue | `#7db8d4` |
| Green | `#8fc06a` |
| Purple | `#a78bda` |
| Orange | `#e8a84c` |

#### 배너 그라디언트
| 슬라이드 | 그라디언트 |
|---------|-----------|
| #1 | `linear-gradient(135deg, #0a0a0a → #1a1a2e → #16213e)` |
| #2 | `linear-gradient(135deg, #0a0a0a → #1b2838 → #0d1b2a)` |
| #3 | `linear-gradient(135deg, #0a0a0a → #2d1b3d → #1a0a2e)` |

### 4-2. 타이포그래피

#### 폰트
| 용도 | 폰트 | 비고 |
|------|------|------|
| 본문 (Primary) | **Pretendard** | 한글 메인 |
| 디스플레이 | **Playfair Display** | 영문 제목/배너 |
| 한글 보조 | Noto Sans KR | 폴백 |

#### 텍스트 크기 스케일
| 이름 | 크기 | 용도 |
|------|------|------|
| text-xs | 12px | 캡션, 메타 정보 |
| text-sm | 14px | 설명 텍스트, 사이드바 |
| text-base | 16px | 본문 기본 |
| text-lg | 18px | 본문 강조 |
| text-xl | 20px | 소제목 |
| text-2xl | 24px | 섹션 제목 |
| text-3xl | 32px | 페이지 제목 |
| text-4xl | 40px | 배너 타이틀 |
| text-5xl | 56px | 히어로 타이틀 |

### 4-3. 간격 (Spacing)

| 이름 | 크기 | 용도 |
|------|------|------|
| space-xs | 4px | 아이콘 간격 |
| space-sm | 8px | 인라인 요소 |
| space-md | 16px | 기본 간격 |
| space-lg | 24px | 섹션 내부 패딩 |
| space-xl | 32px | 섹션 간 간격 |
| space-2xl | 48px | 큰 섹션 간격 |
| space-3xl | 64px | 페이지 레벨 |

### 4-4. 라운드 (Border Radius)

| 이름 | 크기 | 용도 |
|------|------|------|
| radius-sm | 4px | 작은 버튼, 태그 |
| radius-md | 8px | 카드, 메모 블럭 |
| radius-lg | 12px | 패널 |
| radius-xl | 16px | 큰 카드 |
| radius-full | 9999px | 원형 버튼 |

---

## 5. 페이지별 상세 스펙

### 5-1. 랜딩 페이지 (index.html)

**구조:**
```
┌──────────────────────────────────────────────┐
│  헤더 (높이 10vh)                              │
│  [SP 로고]                      [≡ 메뉴 버튼]  │
├──────────────────────────────────────────────┤
│                                              │
│  메인 영역 (높이 90vh) — 배너 캐러셀              │
│                                              │
│   태그: "Discover Your Senses"               │
│   제목: "감각을 깨우는                          │
│         새로운 경험"                           │
│   설명: "SensePoint는..."                     │
│   [CTA 버튼: Explore Now]                     │
│                                              │
│                          [← 01 / 03 →]       │
│                          (우측 하단 네비게이션)   │
└──────────────────────────────────────────────┘
```

**디자인 포인트:**
- 배너는 3장의 풀스크린 캐러셀 (좌우 드래그 + 화살표 이동)
- 배경은 그라디언트 (이미지 사용도 가능)
- 물결(Water Ripple) 효과가 위에 겹침 (Canvas) — 디자인에서는 무시해도 됨
- 텍스트는 좌측 정렬 (모바일에서는 중앙 정렬)
- 네비게이션 화살표 + 카운터는 우측 하단 (모바일에서는 하단 중앙)

### 5-2. Sense Makers (sense-makers.html)

**구조:**
```
┌──────────────────────────────────────────────┐
│  헤더 (10vh)                                  │
├────────────────────────┬─────────────────────┤
│                        │                     │
│  좌측 75%              │  우측 25%             │
│  아티클 본문            │  메모 사이드바          │
│                        │                     │
│  [카테고리 태그]         │  ┌─────────────┐    │
│  제목 (큰 글씨)         │  │ ▚ 메모 블럭 #1 │    │
│  작성자 · 날짜          │  │ "인용 텍스트" │    │
│                        │  │ 메모 내용     │    │
│  본문 텍스트 ...        │  │        26.02 │    │
│  (드래그하여 메모 생성)   │  └─────────────┘    │
│                        │  ┌─────────────┐    │
│                        │  │ 메모 블럭 #2  │    │
│                        │  └─────────────┘    │
│                        │      [ + ]          │
└────────────────────────┴─────────────────────┘
```

**디자인 포인트:**
- 2컬럼 레이아웃 (PC: 75/25, 태블릿: 70/30, 모바일: 단일 컬럼)
- 메모 블럭은 **포스트잇 느낌** — 따뜻한 톤, 그림자, 왼쪽 상단 접힌 코너
- 메모가 없을 때: "Memo" 타이틀(24pt) + 설명(14pt) + "+" 버튼
- 메모 생성 후: 타이틀/설명 숨김, 메모 블럭만 표시
- 하이라이트 색상: 옅은 노란색(기본) / 진한 노란색(선택 중)
- 메모 블럭 기본 크기: 사이드바에 5개가 들어가는 크기
- 메모 블럭 선택 시: 10줄 텍스트가 보이는 크기로 확장

### 5-3. Square (square.html)

**구조:**
```
┌──────────────────────────────────────────────┐
│  헤더 (10vh)                                  │
├────────────────────────┬─────────────────────┤
│  피드 (75%)             │  작성자 사이드바 (25%) │
│                        │                     │
│  [모두 보기] [친구 메모]  │  작성자 아바타         │
│                        │  작성자 이름           │
│  ┌──────────────────┐  │  "n개의 메모"          │
│  │ 아바타 | 작성자    │  │                     │
│  │ 시간 | 아티클 제목  │  │  ┌───────────────┐ │
│  │ [인용 텍스트]      │  │  │ 메모 카드       │ │
│  │ 메모 내용          │  │  │ 시간 | 공개범위  │ │
│  │ 공개범위 태그       │  │  │ [인용 텍스트]   │ │
│  │ — 답글들 —         │  │  │ 메모 내용       │ │
│  │ 메모에 메모하기     │  │  │ 메모에 메모하기  │ │
│  └──────────────────┘  │  └───────────────┘ │
│  ...                   │  ...                │
└────────────────────────┴─────────────────────┘
```

**디자인 포인트:**
- 좌측 피드: 메모 카드 리스트 (시간순, 최신이 위)
- 우측 사이드바: 좌측에서 메모 클릭 시 해당 작성자의 모든 메모 표시
- 필터 탭: "모두 보기" / "친구 메모 보기"
- 각 메모에 공개 범위 표시 (private/friends/public)
- "메모에 메모하기" 기능 — 답글 형태

### 5-4. My Note (my-note.html) — 기획 미완, 디자인 제안 환영

**현재 아이디어:**
- Sense Makers에서 만든 메모를 모아보는 공간
- 독립적인 노트 작성도 가능
- 리스트/카드/타임라인 등 형태는 자유롭게 제안 가능

### 5-5. 로그인 / 회원가입

**로그인 (login.html):**
- 이메일 + 비밀번호
- 회원가입 링크

**회원가입 (signup.html):**
- 이메일, 비밀번호, 닉네임
- "최근 감명받은 이야기" (선택)
- "좋아하는 이야기" (선택)
- 이메일 인증 필요 안내

---

## 6. 공통 컴포넌트

### 6-1. 헤더

```
┌──────────────────────────────────────────────┐
│  [SP 로고 + SensePoint]         [≡ 메뉴 버튼]  │
└──────────────────────────────────────────────┘
```
- 높이: 화면의 10% (모든 페이지에서 상단 고정)
- 배경: 반투명 or 블러 (자유롭게 제안)
- 메뉴 버튼: 햄버거 아이콘 (열면 X로 전환)
- 모바일: 로고 텍스트 숨기고 아이콘만 표시 가능

### 6-2. 메뉴 오버레이 (우측 슬라이드)

```
┌──────────────────────┬───────────┐
│                      │           │
│   backdrop (75%)     │ 메뉴(25%) │
│   (반투명 검정)       │           │
│                      │ [로고]    │
│                      │ [로그인]  │
│                      │           │
│                      │ My Note   │
│                      │ Sense..   │
│                      │ Square    │
│                      │           │
│                      │ © 2026    │
│                      │ contact@  │
│                      │ 02-1234   │
└──────────────────────┴───────────┘
```
- PC: 너비 25% / 태블릿: 35% / 모바일: 80%
- 우측에서 슬라이드 인
- 상단: 로고 + 로그인 버튼
- 중간: 네비게이션 (My Note, Sense Makers, Square)
- 하단 10%: 브랜드 정보 (© 2026, 이메일, 전화번호)

### 6-3. 메모 블럭 (포스트잇)

```
┌──┬──────────────────┐
│▚ │                  │  ← 왼쪽 상단 접힌 코너 (14px)
├──┴──────────────────┤
│ "인용된 텍스트..."     │  ← 발췌 (있을 경우)
│ 메모 내용             │  ← 입력 영역 (경계선 없음)
│                      │
│                26.02 │  ← 날짜 (10px, 우측 하단)
└─────────────────────┘
```
- **2가지 상태** 디자인 필요:
  - Default: 접혀있는 상태 (작은 크기)
  - Selected: 펼쳐진 상태 (10줄 텍스트 보이는 크기)
- 왼쪽 상단 접힌 코너: 드래그 핸들 역할
- 상단 20px 영역: 선택 해제 존 (호버 시 음영 구분)

---

## 7. 피그마 작업 시 지켜주면 좋은 것들 ⭐

### 7-1. Auto Layout 사용
- 모든 프레임에 **Auto Layout** 적용해줘
- 코드가 Flexbox 기반이라 Auto Layout이 1:1로 대응됨
- 고정값(px)보다 **비율(%)** 기반으로 만들어줘

### 7-2. 프레임/레이어 이름 규칙
피그마에서 이 이름 규칙을 따르면 코드 적용이 쉬워:

| 피그마 프레임 이름 | 대응하는 코드 |
|-----------------|-------------|
| `Header` | `.header` |
| `Header/Logo` | `.header__logo` |
| `Header/MenuBtn` | `.header__menu-btn` |
| `MenuPanel` | `.menu-panel` |
| `Landing/Banner` | `.landing__banner` |
| `Landing/Banner/Slide` | `.landing__banner-slide` |
| `SenseMakers/Article` | `.sense-makers__article` |
| `SenseMakers/Sidebar` | `.sense-makers__sidebar` |
| `MemoBlock` | `.memo-block` |
| `MemoBlock/Fold` | `.memo-block__fold` |
| `MemoBlock/Content` | `.memo-block__content` |
| `Square/Feed` | `.square__feed` |
| `Square/AuthorSidebar` | `.square__author-sidebar` |

> 꼭 이 이름이 아니어도 되지만, **일관된 네이밍**을 쓰면 OK.

### 7-3. 색상/텍스트를 피그마 스타일로 등록
- 색상 팔레트를 **Color Styles**로 등록
- 텍스트 크기/폰트를 **Text Styles**로 등록
- 나중에 변경사항을 한눈에 확인 가능

### 7-4. 컴포넌트화
다음 요소들은 피그마 **Component**로 만들어줘 (재사용):
- 헤더
- 메뉴 오버레이
- 메모 블럭 (Default / Selected 상태)
- 메모 추가 버튼 (+)
- 필터 탭 (모두 보기 / 친구 메모)
- 메모 카드 (Square용)

### 7-5. 인터랙션 상태 표시
가능하면 아래 상태별 디자인도 포함:
- **Default** / **Hover** / **Active** / **Disabled**
- 메모 블럭: Default / Selected / 드래그 중
- 버튼: Default / Hover / Active

---

## 8. 디자인 완료 후 전달 방법

완성된 디자인을 코드에 적용할 때 아래 정보가 필요해:

### 방법 1: 스크린샷 + 수치 (가장 간편)
1. 각 페이지/컴포넌트 **스크린샷** 캡처
2. 변경된 색상값, 폰트 크기, 간격 등 **주요 수치** 메모
3. 스크린샷과 수치를 함께 전달

### 방법 2: 피그마 Dev Mode (가장 정확)
1. 피그마에서 **Dev Mode** 활성화 (`</>` 버튼)
2. 각 요소의 **CSS 값** 복사 가능
3. 공유 링크 전달하면 코드에서 직접 참조 가능

### 방법 3: 피그마 공유 링크
1. 피그마 파일 **View 권한 공유**
2. 링크만 전달하면 Cursor AI가 참고하여 적용

---

## 9. 자유도가 높은 영역 (기민이 재량)

아래 항목들은 현재 임시값이므로 자유롭게 디자인해줘:

- [ ] **로고 디자인** — 현재 "SP" 텍스트만 있음, 로고 이미지/아이콘 제안 환영
- [ ] **배너 비주얼** — 현재 그라디언트만 있음, 이미지/일러스트 추가 가능
- [ ] **메모 블럭 디테일** — 포스트잇 느낌의 구체적인 시각 표현
- [ ] **My Note 페이지** — 기획이 미완이므로 디자인부터 제안 가능
- [ ] **마이크로 인터랙션** — 호버 효과, 전환 애니메이션 등
- [ ] **빈 상태(Empty State)** — 메모가 없을 때, 피드가 비었을 때 등
- [ ] **에러 상태** — 로그인 실패, 네트워크 오류 등
- [ ] **라이트 모드** — 현재는 다크 모드만 있음, 라이트 모드 제안 가능

---

## 10. 참고: 현재 구현된 화면 확인 방법

프로젝트 파일을 받아서 `index.html`을 브라우저에서 열면 현재 상태를 확인할 수 있어.
또는 스크린샷을 별도로 전달할게.
